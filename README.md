# PRODIGY_GA_TASK1
A text generation project using GPT-2, fine-tuned on a custom dataset of motivational and reflective quotes. The model’s performance is evaluated by comparing outputs before and after training, highlighting improvements in coherence, tone, and relevance.
